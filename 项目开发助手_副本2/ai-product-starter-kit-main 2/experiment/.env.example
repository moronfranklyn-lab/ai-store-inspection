# 复制本文件为 .env，并填入自己的 Key。
# .env 已在 .gitignore 中排除，绝对不要提交、不要贴到对话或截图里。
DASHSCOPE_API_KEY=

# 可选覆盖项
OOS_MODEL=qwen-vl-max
# OOS_SAMPLES=3
# OOS_MIN_GAPS=3
# OOS_TIMEOUT=60
# OOS_MAX_RETRIES=3
# OOS_PHOTOS_DIR=/absolute/path/to/photos
